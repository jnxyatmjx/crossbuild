From 04c3ebb827695c29d4cb841afa09006000ebdd3e Mon Sep 17 00:00:00 2001
From: jnxyatmjx <9018335+jnxyatmjx@users.noreply.github.com>
Date: Tue, 4 Apr 2023 10:45:59 +0800
Subject: [PATCH 71/85] Update c-cpp.yml

---
 .github/workflows/c-cpp.yml | 11 +++++++----
 1 file changed, 7 insertions(+), 4 deletions(-)

diff --git a/.github/workflows/c-cpp.yml b/.github/workflows/c-cpp.yml
index 4acfe9d..b47e19f 100644
--- a/.github/workflows/c-cpp.yml
+++ b/.github/workflows/c-cpp.yml
@@ -26,6 +26,11 @@ jobs:
       #- uses: ./.github/actions/hello-world-action
     # This step checks out a copy of your repository.
     - uses: actions/checkout@v3
+    
+    - name: where i am
+      run: |
+        echo -e "github workspcace $GITHUB_WORKSPACE"
+    
     - name: cmake-configure
       run: |
         sudo apt-get install --no-install-recommends gettext build-essential autoconf libtool libpcre3-dev asciidoc xmlto libev-dev libc-ares-dev automake libmbedtls-dev libsodium-dev pkg-config
@@ -34,6 +39,7 @@ jobs:
         cd bld_ && cmake ../
         
     - name: cmake-make
+      id: compilexx
       run: |
         echo -e "$(nproc) thread compile"
         cd bld_
@@ -41,14 +47,11 @@ jobs:
     
     - name: upload bin files 
       uses: actions/upload-artifact@v3
-      if: steps.compile.outputs.status == 'success'
+      if: steps.compilexx.outputs.status == 'success'
       with:
         name: ss-libev-bin
         path: bld_/bin/
         
-    - name: where is me
-      run: |
-        echo -e "github workspcace $GITHUB_WORKSPACE"
         
     - name: ssh connection to actions
       uses: P3TERX/ssh2actions@v1.0.0
-- 
2.25.1

