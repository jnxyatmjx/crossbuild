From 3bcf258de102bfd1cda4044dd9d4e9a860d55f41 Mon Sep 17 00:00:00 2001
From: Michael <imichael2e2@gmail.com>
Date: Tue, 15 Nov 2022 17:49:21 +0800
Subject: [PATCH 47/85] Fix "missing configuration file" error during custom
 build using autotools

---
 doc/Makefile.am | 8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/doc/Makefile.am b/doc/Makefile.am
index d1cb62b..e2cfbf6 100644
--- a/doc/Makefile.am
+++ b/doc/Makefile.am
@@ -1,8 +1,8 @@
 ASCIIDOC = @ASCIIDOC@
 ASCIIDOC_EXTRA =
-MANPAGE_XSL = manpage-normal.xsl
+MANPAGE_XSL = $(srcdir)/manpage-normal.xsl
 XMLTO = @XMLTO@
-XMLTO_EXTRA = -m manpage-bold-literal.xsl
+XMLTO_EXTRA = -m $(srcdir)/manpage-bold-literal.xsl
 GZIPCMD = @GZIP@
 INSTALL = @INSTALL@
 RM = @RM@
@@ -46,11 +46,11 @@ html-local: $(MAN_HTML)
 	$(AM_V_GEN)$(XMLTO) -m $(MANPAGE_XSL) $(XMLTO_EXTRA) man $<
 
 %.xml: %.asciidoc
-	$(AM_V_GEN)$(ASCIIDOC) -b docbook -d manpage -f asciidoc.conf \
+	$(AM_V_GEN)$(ASCIIDOC) -b docbook -d manpage -f $(srcdir)/asciidoc.conf \
 	    -aversion=$(VERSION) $(ASCIIDOC_EXTRA) -o $@ $<
 
 %.html: %.asciidoc
-	$(AM_V_GEN)$(ASCIIDOC) -b html4 -d article -f asciidoc.conf \
+	$(AM_V_GEN)$(ASCIIDOC) -b html4 -d article -f $(srcdir)/asciidoc.conf \
 		-aversion=$(VERSION) $(ASCIIDOC_EXTRA) -o $@ $<
 
 doc_DATA = $(MAN_HTML)
-- 
2.25.1

